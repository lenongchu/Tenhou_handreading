# 项目迁移指南

## 背景

当前项目位于OneDrive同步文件夹中，随着数据量增长（预计最终可达100+ GB），需要迁移到本地E盘以避免：
- OneDrive同步压力
- 云存储空间不足
- 同步冲突

## 当前状态

- **当前路径**: `E:\Cursor\Tenhou_handreading`（已从 D 盘 OneDrive 迁移）
- **当前数据库大小**: 2.44 GB
- **已下载对局**: 268,831 条（17.41%）
- **预计最终大小**: 130+ GB（处理全部数据后）

## 迁移步骤

### 1. 运行迁移工具

```bash
cd "E:\Cursor\Tenhou_handreading"
py -3.12 migrate_to_e_drive.py
```

工具会自动：
- ✅ 检查E盘可用空间
- ✅ 复制整个项目到 `E:\Tenhou_handreading`
- ✅ 验证文件完整性
- ✅ 保留OneDrive中的原始文件

预计耗时：约 2-3 分钟（取决于磁盘速度）

### 2. 验证迁移结果

```bash
cd E:\Tenhou_handreading
py -3.12 verify_migration.py
```

验证内容：
- ✅ 数据库文件存在且可访问
- ✅ 所有Python模块可正常导入
- ✅ 关键文件完整
- ✅ 工作目录在E盘

### 3. 更新IDE工作路径

#### Cursor:
1. File -> Open Folder
2. 选择 `E:\Cursor\Tenhou_handreading`
3. 关闭旧的OneDrive路径窗口

### 4. 恢复后台下载（如果正在运行）

如果之前正在后台下载数据：

1. **停止OneDrive路径中的下载**：
   - 找到正在运行的命令行窗口
   - 按 `Ctrl+C` 停止

2. **在新路径重新启动**：
   ```bash
   cd E:\Cursor\Tenhou_handreading
   后台下载数据.bat
   ```

3. **验证下载继续**：
   ```bash
   py -3.12 check_db_status.py
   ```

### 5. 清理OneDrive旧文件（可选）

**⚠️ 重要：请在完全确认新路径工作正常后再删除！**

建议等待几天，确保：
- 新路径项目运行正常
- 数据下载正常继续
- 没有遗漏的文件

然后手动删除：
```
D:\OneDrive - City University of Hong Kong - Student\Cursor\Tenhou_handreading
```

## 常见问题

### Q1: 迁移后数据库是否需要重建索引？
**A**: 不需要。SQLite数据库文件是完整的，迁移后直接可用。

### Q2: 迁移会中断正在运行的下载吗？
**A**: 是的。需要先停止下载，迁移后重新启动。`houou-logs`支持断点续传，不会重复下载。

### Q3: 如果迁移失败怎么办？
**A**: 原始文件保留在OneDrive中，直接删除E盘的不完整文件，重新运行迁移工具即可。

### Q4: 迁移后OneDrive还会同步什么？
**A**: 迁移后，只有你手动放在OneDrive中的小文件（如代码）会同步，大数据文件在E盘本地。

### Q5: 如何在两台电脑间同步代码？
**A**: 
- **推荐**：使用Git进行代码版本控制
- **备选**：只同步源代码文件夹（src/），数据文件各自本地

## 目录结构对比

### 迁移前（D 盘 OneDrive）：
```
D:\OneDrive - City University of Hong Kong - Student\Cursor\Tenhou_handreading\
├── data\
│   └── tenhou.db (2.44 GB, 会同步到OneDrive)
├── src\
├── *.py
└── ...
```

### 迁移后（E 盘）：
```
E:\Cursor\Tenhou_handreading\
├── data\
│   └── tenhou.db (2.44 GB -> 130+ GB, 本地存储)
├── src\
├── *.py
└── ...
```

## 性能预期

| 操作 | OneDrive路径 | E盘路径 | 提升 |
|------|-------------|---------|------|
| 数据库查询 | 50-100ms | 10-20ms | 5x |
| 数据写入 | 受同步影响 | 直接写入 | 10x+ |
| 下载速度 | 可能冲突 | 无冲突 | 稳定 |

## 备份建议

迁移到E盘后，建议定期备份：
1. **代码**: 使用Git + GitHub
2. **数据库**: 
   - 每周备份到外部硬盘
   - 或使用 `robocopy` 自动增量备份

示例备份命令：
```bash
robocopy E:\Cursor\Tenhou_handreading F:\Backup\Tenhou_handreading /MIR /Z /R:3
```

## 技术支持

如遇到问题，检查：
1. `verify_migration.py` 的输出
2. `check_db_status.py` 的数据统计
3. 日志文件中的错误信息
