# 麻将读牌等价性分析（正确版本）

## 核心原则

在读牌分析中，**出牌顺序和具体数字位置都很重要**，因为它们反映了不同的手牌结构和思考过程。

## 等价性定义

两个舍牌序列等价当且仅当：
1. **花色可以互换**（1m ≡ 1p ≡ 1s，三种数牌花色等价）
2. **可以镜像**（1s ≡ 9s, 2s ≡ 8s，保持相对位置）
3. **顺序必须相同**（出牌顺序不能改变）

## 等价的情况

### 1. 花色等价

不同花色的相同数字模式是等价的：

```
1m-2m ≡ 1p-2p ≡ 1s-2s
3m-1m ≡ 3p-1p ≡ 3s-1s
1m-3m-5m ≡ 1p-3p-5p ≡ 1s-3s-5s
```

**原因**：从读牌角度，打1m-2m和打1p-2p反映的手牌结构是相同的，只是花色不同。

### 2. 镜像等价

数字镜像（1↔9, 2↔8, 3↔7, 4↔6, 5↔5）且保持顺序的模式是等价的：

```
1s-2s ≡ 9s-8s（镜像：1→9, 2→8）
3s-1s ≡ 7s-9s（镜像：3→7, 1→9）
1s-3s ≡ 9s-7s（镜像：1→9, 3→7）
2s-4s-6s ≡ 8s-6s-4s（镜像：2→8, 4→6, 6→4）
```

**原因**：从读牌角度，打1s-2s（边张）和打9s-8s（边张）反映的手牌结构是对称的。

### 3. 花色 + 镜像组合

可以同时应用花色转换和镜像：

```
1m-2m 的所有等价形式（6种）：
- 1m-2m, 1p-2p, 1s-2s（花色）
- 9m-8m, 9p-8p, 9s-8s（镜像+花色）

3m-1m 的所有等价形式（6种）：
- 3m-1m, 3p-1p, 3s-1s（花色）
- 7m-9m, 7p-9p, 7s-9s（镜像+花色）
```

## 不等价的情况

### 1. 平移不等价 ❌

**不同数字位置的模式不等价**，即使间隔相同：

```
1s-2s ≠ 2s-3s ≠ 3s-4s ≠ ... ≠ 8s-9s
1s-3s ≠ 2s-4s ≠ 3s-5s ≠ ... ≠ 7s-9s
```

**原因**：
- `1s-2s`：边张（12s搭子），可能手里有3s或更多中张
- `2s-3s`：中张（23s搭子），可能手里有1s、4s或其他中张
- `8s-9s`：边张（89s搭子），可能手里有7s或更多中张

这三种情况反映的手牌结构完全不同，对其他牌的推测也不同。

### 2. 顺序不等价 ❌

**出牌顺序改变后不等价**：

```
1s-3s ≠ 3s-1s
3m-1m ≠ 1m-3m
1s-2s-3s ≠ 3s-2s-1s
```

**原因**：
- `3m-1m`：先打3m再打1m，可能先拆中张搭子，再打边张
- `1m-3m`：先打1m再打3m，可能先打边张，再拆中张搭子

出牌顺序反映了手牌演变过程和玩家的思考，是读牌的重要信息。

### 3. 间隔不等价 ❌

**数字间隔不同的模式不等价**：

```
1s-2s ≠ 1s-3s
1s-2s-3s ≠ 1s-3s-5s
```

**原因**：间隔1（搭子）和间隔2（孤张）反映完全不同的手牌状态。

## 标准化算法

### 实现策略

```python
def normalize_discard_pattern(tiles):
    """
    标准化舍牌序列
    
    步骤：
    1. 统一花色为 s
    2. 生成镜像序列
    3. 选择字典序最小的（原始 vs 镜像）
    
    注意：不进行平移！
    """
    # 候选1: 原始序列（只统一花色）
    original = [f"{num}s" for num in tiles]
    
    # 候选2: 镜像序列
    mirrored = [f"{10-num}s" for num in tiles]
    
    # 返回字典序最小的
    return min(["-".join(original), "-".join(mirrored)])
```

### 标准化示例

```python
normalize("1m-2m") → "1s-2s"
normalize("9m-8m") → "1s-2s"  # 镜像后
normalize("2m-3m") → "2s-3s"  # 不平移！
normalize("3m-1m") → "3s-1s"
normalize("7m-9m") → "3s-1s"  # 镜像后
normalize("1m-3m") → "1s-3s"
normalize("9m-7m") → "1s-3s"  # 镜像后
```

## 为什么不平移？

### 错误的想法 ❌

某些分析可能认为：
```
1s-2s ≡ 2s-3s ≡ 3s-4s（平移）
→ 都标准化为 1s-2s
```

### 为什么错误？

1. **手牌结构不同**：
   - 打1s-2s：可能是 [3s, 4s, 5s, ...] 的手牌（有中张）
   - 打2s-3s：可能是 [1s, 4s, 5s, ...] 的手牌（有边张和中张）
   - 打8s-9s：可能是 [6s, 7s, ...] 的手牌（有中张）

2. **对其他牌的推测不同**：
   - 打过1s-2s的玩家，手里可能有3s
   - 打过2s-3s的玩家，手里可能有1s或4s
   - 这是读牌的关键信息！

3. **统计样本会混淆**：
   - 如果将1s-2s和2s-3s混在一起统计
   - 查询"打1s-2s的玩家有几张3s"时
   - 会错误地包含"打2s-3s"的玩家（他们的3s分布不同）

## 实际应用示例

### 查询："3m-1m" 后手里有几张 "2m"？

**匹配的等价模式（6种）**：
```
3m-1m, 3p-1p, 3s-1s  （花色）
7m-9m, 7p-9p, 7s-9s  （镜像）
```

**不匹配的模式**：
```
1m-3m  （顺序不同）
2m-4m  （平移，数字位置不同）
4m-2m  （顺序和数字都不同）
```

**统计目标**：
- 对于 3m-1m：统计手里的 2m
- 对于 7m-9m：统计手里的 8m（镜像）
- 对于 3p-1p：统计手里的 2p（花色）
- 对于 7s-9s：统计手里的 8s（镜像+花色）

## 测试验证

### 正确的等价性（应该匹配）

```python
assert normalize("3m-1m") == normalize("3s-1s")  # 花色
assert normalize("3m-1m") == normalize("7m-9m")  # 镜像
assert normalize("3m-1m") == normalize("7p-9p")  # 镜像+花色
```

### 不等价（不应该匹配）

```python
assert normalize("3m-1m") != normalize("1m-3m")  # 顺序
assert normalize("3m-1m") != normalize("4m-2m")  # 平移
assert normalize("3m-1m") != normalize("2m-4m")  # 平移+顺序
assert normalize("1s-2s") != normalize("2s-3s")  # 平移
assert normalize("1s-2s") != normalize("8s-9s")  # 平移
```

## 总结

### ✅ 等价的操作
1. 花色转换（m ↔ p ↔ s）
2. 镜像（1↔9, 2↔8, ...）
3. 两者组合

### ❌ 不等价的操作
1. 平移（1s-2s ≠ 2s-3s）
2. 改变顺序（3m-1m ≠ 1m-3m）
3. 改变间隔（1s-2s ≠ 1s-3s）

### 设计原则

**保留所有对读牌有意义的信息**：
- 数字位置（边张/中张/幺九）
- 出牌顺序（手牌演变过程）
- 数字间隔（搭子/孤张）

**只消除对读牌无意义的差异**：
- 花色差异（1m和1s的读牌意义相同）
- 镜像对称（1s和9s的边张性质相同）
