
Plan

即时铳率/铳点/铳度功能实施方案（独立分页）
1. 目标与范围
新增一个独立“铳率分析”分页，不复用“和铳率(整局结局)”统计口径。
统计口径改为：在“命中舍牌模式 + 约束条件”的当巡瞬间，针对用户输入目标牌，计算对该模式玩家本人的：
铳率 = 可荣和样本数 / 总匹配样本数
铳点 = 可荣和样本的理论点数均值
铳度 = 铳率 * 铳点
规则默认按“天凤默认”（你已确认），并实现完整振听（舍张振听 + 同巡振听 + 立直振听）。
2. 与现有功能的边界
现有 analysis_target="outcome" 保留不动（整局和了/放铳结局）。
新增 analysis_target="deal_in_instant"（命名可内部使用），只看命中瞬间，不看终局结果。
现有“目标牌存量/是否听牌”逻辑不改，避免回归风险。
3. 核心实现设计
3.1 新分析入口（LiveAnalyzer）
在 LiveAnalyzer 新增 compute_instant_deal_in_metrics(...)。
输入参数复用现有查询参数，并新增/强化：
target_tile 必填且只能是单张（非 multi-target/非 combo）。
输出结果新增字段：
total_matches, deal_in_hits, deal_in_rate
deal_in_point_sum, deal_in_point_avg
deal_in_intensity
analysis_target="deal_in_instant"
sample_pool 中增加：deal_in_hit, deal_in_point, furiten_state, furiten_reason, waits_snapshot（用于验样解释）。
3.2 当巡“可荣和”判定引擎
新增模块（建议 src/instant_deal_in.py），按 round 的事件流重放状态。
使用 tenhou6 game[] 原始事件顺序，不依赖仅有 GameState.discards 的简化视图。
对每个玩家维护：当前手牌、已副露、立直状态、本人牌河、振听状态（临时/永久）。
对每次他家 dahai 事件，更新三家“是否出现可荣和而未和”的状态，用于同巡/立直振听。
对命中样本所在事件点，判断该玩家是否对 target_tile 可荣和：
必须满足：和牌形成立、存在役、非振听。
理论点使用 mahjong 库 HandCalculator 按天凤默认规则计算 ron 点数。
3.3 完整振听判定规则
舍张振听：当前可荣和张与本人历史舍牌有交集则振听。
同巡振听：本巡内出现可荣和牌但未和，至下一次自摸前不可荣和。
立直振听：立直后若出现可荣和牌未和，之后本局不可荣和。
重放时记录“触发原因”，用于样本展示。
3.4 点数与役
“有役”不单独手写规则，统一以 HandCalculator 是否可荣和为准。
铳点取该目标牌对应的理论 ron 点（单次样本一个值）。
若存在多种和牌分解，取库返回的最优可和结果。
红宝、里宝、食断等使用天凤默认配置。
4. GUI 方案（独立分页）
在主 QTabWidget 新增“铳率分析”页（不塞进原查询页的 analysis_target_combo）。
该页从现有查询页克隆，改动为：
强制需要“目标牌”输入（单张）。
不提供“和铳率(outcome)”项。
结果区显示四个核心值：总样本、铳率、平均铳点、铳度。
样本筛选改为：全部 / 可铳 / 不可铳 / 振听过滤掉。
样本详情新增“振听原因、当时待牌、理论点”。
5. 性能与工程约束
仅在新分页触发“事件流重放+完整振听”，避免拖慢现有分析。
round 内计算做缓存：
ron_check_cache[(round_id, player_id, event_idx, tile)]
避免重复手牌求值。
保持并行框架兼容：每 log worker 内独立重放，不共享可变状态。
6. 测试与验收
6.1 单元测试
舍张振听：本人打过待牌应不可荣和。
同巡振听：漏和后至下次自摸前不可荣和，自摸后恢复。
立直振听：立直后漏和永久不可荣和。
有役/无役边界：同形但无役不可计入。
理论点计算：普通手、满贯、役满至少各 1 例。
6.2 集成测试
与现有模式并存：target_count/tenpai/outcome 输出不变。
新模式返回结构完整，铳率=hits/total、铳度=铳率*铳点 数学一致。
多模式/等价变体下映射后 target_tile 判定正确。
6.3 GUI 验收
新分页可独立发起分析、取消、查看进度、导出样本。
旧分页行为与文案不回归。
样本弹窗能解释“为什么可铳/不可铳”。
7. 公开接口/类型变更
live_analyzer.py
新增 compute_instant_deal_in_metrics(...)
扩展结果字典字段（见 3.1）。
gui_app.py
新增“铳率分析”分页与对应线程类（可复用 QueryThread 框架）。
新增样本展示字段渲染。
新增模块 instant_deal_in.py（事件重放 + 振听 + 理论点）。
8. 默认假设（已锁定）
统计对象是“模式玩家本人”的被铳概率与点数（你已确认）。
统计时点是“命中模式与约束的当巡瞬间”（你已确认）。
铳点使用“即时理论点”，不是终局实际点（你已确认）。
规则使用“天凤默认”。
振听按完整规则实现。
